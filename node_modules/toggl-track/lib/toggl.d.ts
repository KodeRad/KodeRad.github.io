import { Me } from './me';
import { TimeEntry } from './timeEntry';
import { Invitations } from './invitations';
import { Projects } from './projects';
import { Tags } from './tags';
export declare class Toggl {
    me: Me;
    timeEntry: TimeEntry;
    invitations: Invitations;
    projects: Projects;
    tags: Tags;
    private axios;
    constructor({ auth, baseURL, axiosConfig, }: {
        auth: Auth;
        baseURL?: string;
        axiosConfig?: any;
    });
    request<T = unknown>(endpoint: string, { body, query, method, axiosConfig, }?: {
        body?: object;
        query?: Record<string, string | number | boolean | null | undefined>;
        method?: string;
        axiosConfig?: any;
    }): Promise<any>;
    private authHeader;
}
export interface BasicAuth {
    email: string;
    password: string;
}
export interface ApiToken {
    token: string;
}
export declare type Auth = BasicAuth | ApiToken;
